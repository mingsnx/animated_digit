name: animated_digit
description: A scrolling digital animation widget that can be used to display the amount of movement, the number of people online in real time, Any number that requires animation effects is easy to use and easy to control, and it can also ensure the accuracy of the amount of calculation.
version: 1.0.3
homepage: https://github.com/mingsnx/animated_digit

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
